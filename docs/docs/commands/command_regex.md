# regex

Parse a field with a regular expression
## Aliases

* `regex`
* `rex`
* `rx`

## Synopsis

```shell
| regex {src} with <regular expression> [as|to {dest}]
```

## Schema

```json
{
  "properties": {
    "{dest}": {
      "type": "object"
    }
  }
}
```

## Description

## Examples
